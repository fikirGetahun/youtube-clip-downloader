# __init__.py for backend package
# This file ensures that the 'backend' folder is treated as a package.
# It can also include shared imports or setup code if necessary.

# Example: shared imports
from .downloader import VideoDownloader
from .threading_manager import ThreadingManager
 